#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../microdatabase/bin/microdatabase" "$@"
  ret=$?
else 
  node  "$basedir/../microdatabase/bin/microdatabase" "$@"
  ret=$?
fi
exit $ret
